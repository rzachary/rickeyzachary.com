<header>
  <div class="navbar fixed-top navbar-expand-lg navbar-light whie scrolling-navbar">
    <div class="header header-global-top header-title">
      {%- assign default_paths = site.pages | map: "path" -%}
      {%- assign page_paths = site.header_pages | default: default_paths -%}
       <a class="site-title" href="{{ "http://rickeyzachary.com" / relative_url }}">
          <h1 class="header-global-logo">
				    <span class="visually-hidden">{{ site.title | escape }}</span>
            <img src="/assets/logo.svg" type="image/svg+xml"> </img>
					</h1>
			 </a>
    </div>
    {%- if page_paths -%}
    <div class="header header-global-bottom header-title">
      <nav class="global-menu">
        <ul class="global-menu-nav">
          {%- for path in page_paths -%}
            {%- assign my_page = site.pages | where: "path", path | first -%}
            {%- if my_page.title -%}
            <li class="global-nav-menu-item menu-{{ my_page.title | escape }}">
            <a class="page-link" href="{{ my_page.url | relative_url }}">{{ my_page.title | escape }}</a></li>
            {%- endif -%}
          {%- endfor -%}
        </ul>
      </nav>
    </div>
    {%- endif -%}
  </div>
</header>
